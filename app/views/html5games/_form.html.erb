<%= form_for(@html5game) do |f| %>
  <% if @html5game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@html5game.errors.count, "error") %> prohibited this html5game from being saved:</h2>

      <ul>
      <% @html5game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
