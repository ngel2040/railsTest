<%= form_for(@back_bone_test) do |f| %>
  <% if @back_bone_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@back_bone_test.errors.count, "error") %> prohibited this back_bone_test from being saved:</h2>

      <ul>
      <% @back_bone_test.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ToDo %><br />
    <%= f.text_field :ToDo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
